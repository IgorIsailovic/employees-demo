version: '3.8'
services:
  db:
    container_name: igor_homework
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: homework
    ports:
      - "5432:5432"
      